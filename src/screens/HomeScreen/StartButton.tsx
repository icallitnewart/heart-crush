import React from 'react';

import LetterButton from '../../components/LetterButton';

function StartButton(): React.ReactElement {
	return <LetterButton>Start</LetterButton>;
}

export default StartButton;
